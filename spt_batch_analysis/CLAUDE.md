# SPT Batch Analysis — FLIKA plugin

Project navigation map: @INTERFACE.md

- Read `INTERFACE.md` before opening source files.
- Run tests with `python3 -m pytest tests/` (requires the `flika_new` conda env or
  equivalent: numpy, scipy, numba, scikit-image, pyqtgraph, PyQt, flika).
- `PATCH_NOTES_uncertainty.md` documents the 2026-08-07 localization-uncertainty fix
  and its known remaining issue (detection/filtering stage, not the precision formula).
- Update `SESSION_LOG.md` with progress each session.
